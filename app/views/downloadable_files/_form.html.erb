<%= bootstrap_form_for(file) do |form| %>
  <% if file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file.errors.count, "error") %> prohibited this file from being saved:</h2>

      <ul>
        <% file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title %>
  <%= form.text_field :filename %>
  <%= form.file_field :file %>
  <%= form.primary %>
  <%= link_to 'Back', downloads_path, :class => "btn btn-secondary" %>

<% end %>
