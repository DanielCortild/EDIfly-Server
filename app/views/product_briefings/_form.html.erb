<%= bootstrap_form_for(product_briefing) do |form| %>
  <% if product_briefing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_briefing.errors.count, "error") %> prohibited this product briefing from being saved:</h2>

      <ul>
        <% product_briefing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title %>
  <%= form.text_field :filename %>
  <%= form.file_field :file %>
  <%= form.primary %>
  <%= link_to 'Back', downloads_path, :class => "btn btn-secondary" %>

<% end %>
